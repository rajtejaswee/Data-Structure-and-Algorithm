#include<bits/stdc++.h>
using namespace std;

void explainList() {
    list<int> ls;
    ls.push_back(1);
    ls.emplace_back(2);

    ls.push_front(5);
    // rest all the functions are same as of vectors
    // begin, end , rend, rbegin, read, clear, swap, insert, size etc.
}